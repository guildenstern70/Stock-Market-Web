<h1>Selling <%= pluralize(@stocks.length, 'stock') %> </h1>
<p>
  <%= form_tag() do %>
    <table border='1' cellspacing='2' cellpadding='2' width='95%'> 
    <th width='40%'>Company Name</th> 
    <th width='20%'>Current</th> 
    <th width='20%'>You have</th> 
    <th width='20%'>Sell</th> 
        <% @stocks.each do |e| %>
            <tr>
                <td><%= e.title %></td>
                <td><%= e.current %></td>
                <td>0</td>
                <td><%= text_field_tag 'sellqty[]', 0 %></td>
            </tr>
        <% end %>
    </table>
  <p>&nbsp;</p>
  <%= submit_tag 'Cancel', :name => 'commit', :class => "stylebutton" %>
  <%= submit_tag 'Sell', :name => 'commit', :class => "stylebutton" %>
  <% end %>

</p>